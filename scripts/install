#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail

echo "==> Installing dependencies…"
poetry install

# @todo #46 Install ruby packages with bundler.

# @todo #89 Install nodejs packages with npm.
